# Zemzm Etan Bet — Static Vercel Site

This is a static website. The required entry point is `index.html` at the repository root.
No Node.js, npm, build command, framework, database, or server is required.
